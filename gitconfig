[user]
    name = Rafik
    email = bennacer860@gmail.com
[diff]
[color]
    ui = auto
[alias]
    s = status
    ca = commit -am
    co = checkout
    aa = add --all

    # THANK YOU GARRY
    # Fancy logging.
    #   h = head
    #   hp = head with patch
    #   r = recent commits, only current branch
    #   ra = recent commits, all reachable refs
    #   l = all commits, only current branch
    #   la = all commits, all reachable refs
    head = !git r -1
    h = !git head
    hp = "!. ~/.githelpers && show_git_head"
    r = !GIT_NO_PAGER=1 git l -30
    ra = !git r --all
    l = "!. ~/.githelpers && pretty_git_log"
    la = !git l --all

[merge]
    tool = vimdiff


